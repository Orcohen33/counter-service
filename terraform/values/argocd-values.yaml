---
global:
  image:
    tag: "v3.0.12"
crds:
  keep: false

server:
  service:
    type: ClusterIP
  extraArgs:
    - --insecure

controller:
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi
